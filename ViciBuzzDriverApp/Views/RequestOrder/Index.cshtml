@using ViciBuzzDriverApp.Models;
@model IEnumerable<RequestOrderViewModel>
@{
    ViewData["Title"] = "Home Page";
    int index = 1;
}

<div class="row">
                <div class="col-2">
                    OrderId
                </div>
                <div class="col-2">
                    Item
                </div>
                <div class="col-2">
                    Discription
                </div>
            
                @foreach (var item in Model)
                {
                         <div class="row">
                                                                  <div class="col-2">
                @item.Id
                                                                  </div>
                                                                  <div class="col-2">
                @item.Title
                                                                  </div>
                                                                      <div class="col-2">
                @item.Discription
                                                                      </div>
                            </div>
                    
                }
</div>